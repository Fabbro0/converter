<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark, near-black base so the launch window matches the Compose theme
         (no white flash before Compose takes over) — see ui/theme/Theme.kt. -->
    <style name="Theme.MegaConverter" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#000000</item>
        <item name="android:statusBarColor">#000000</item>
        <item name="android:navigationBarColor">#000000</item>
    </style>
</resources>
